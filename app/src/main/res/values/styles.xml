<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="drawerArrowStyle">@style/DrawerStyle</item>
        <item name="fontFamily">@font/rockwell_nova</item>
        <item name="chipStyle">@style/CustomChipStyle</item>
        <item name="android:textColor">@android:color/secondary_text_light</item>
    </style>

    <style name="AppTheme.DarkBrown">
        <item name="colorAccent">@color/colorPrimaryDark</item>
    </style>

    <style name="SplashScreenTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="DrawerStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/white</item>
    </style>

    <style name="MyFAB" parent="@style/Widget.MaterialComponents.FloatingActionButton">
        <item name="materialThemeOverlay">@style/MyFabOverlay</item>
    </style>

    <style name="MyFABWhite" parent="@style/Widget.MaterialComponents.FloatingActionButton">
        <item name="materialThemeOverlay">@style/MyFabOverlayWhite</item>
    </style>

    <style name="MyFabOverlay">
        <!-- color of the background -->
        <item name="colorSecondary">@color/colorPrimary</item>
        <!-- color of the icon -->
        <item name="colorOnSecondary">@color/white</item>
    </style>

    <style name="MyFabOverlayWhite">
        <!-- color of the background -->
        <item name="colorSecondary">@color/white</item>
        <!-- color of the icon -->
        <item name="colorOnSecondary">@color/colorPrimary</item>
    </style>

    <style name="Dialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textColor">@android:color/secondary_text_light</item>
        <item name="android:textColorPrimary">@color/colorPrimaryDark</item>
        <item name="buttonBarPositiveButtonStyle">@style/ButtonPositiveStyle</item>
        <item name="buttonBarNegativeButtonStyle">@style/ButtonNegativeStyle</item>
    </style>

    <style name="ButtonPositiveStyle" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="ButtonNegativeStyle" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="textAllCaps">false</item>
    </style>

    <!-- Chip style -->
    <style name="CustomChipStyle" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="chipBackgroundColor">@color/chip_background_color</item>
        <item name="android:textColor">@color/chip_text_color</item>
        <item name="cornerRadius">4dp</item>
        <item name="cornerFamily">cut</item>
        <item name="shapeAppearanceOverlay">@null</item>
        <item name="android:checkable">true</item>
        <item name="checkedIconVisible">false</item>
    </style>

    <!-- Set title to white when DetailActivity is collapsed -->
    <style name="collapsingToolbarLayoutTitleColor" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="expandedToolbarTitleColor" parent="TextAppearance.Design.CollapsingToolbar.Expanded">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="BottomSheetDialogBackground" parent="Theme.MaterialComponents.BottomSheetDialog">
        <item name="cornerSizeTopLeft">@dimen/margin_24</item>
        <item name="cornerSizeTopRight">@dimen/margin_24</item>
    </style>
</resources>
